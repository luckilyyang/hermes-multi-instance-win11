# 上游服务器定义
upstream admin_dashboard {
    server admin-dashboard:5000;
}

upstream studio_super {
    server studio-super:6060;
}

upstream studio_ls {
    server studio-ls:6060;
}

upstream studio_zs {
    server studio-zs:6060;
}

# ===== 中央管理面板 (admin.xx.local) =====
server {
    server_name admin.xx.local;
    listen 80;

    location / {
        proxy_pass http://admin_dashboard;
        proxy_http_version 1.1;
        
        # 必需的代理头
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $server_name;
        
        # WebSocket 支持
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # Socket.IO 支持
        proxy_buffering off;
        proxy_cache off;
        proxy_request_buffering off;
        
        # 超时设置
        proxy_connect_timeout 7d;
        proxy_send_timeout 7d;
        proxy_read_timeout 7d;
    }
}

# ===== 超级管理员实例 (super.xx.local) =====
server {
    server_name super.xx.local;
    listen 80;

    location / {
        proxy_pass http://studio_super;
        proxy_http_version 1.1;
        
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $server_name;
        
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        proxy_buffering off;
        proxy_cache off;
        proxy_request_buffering off;
        
        proxy_connect_timeout 7d;
        proxy_send_timeout 7d;
        proxy_read_timeout 7d;
    }
}

# ===== LS 管理员实例 (ls.xx.local) =====
server {
    server_name ls.xx.local;
    listen 80;

    location / {
        proxy_pass http://studio_ls;
        proxy_http_version 1.1;
        
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $server_name;
        
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        proxy_buffering off;
        proxy_cache off;
        proxy_request_buffering off;
        
        proxy_connect_timeout 7d;
        proxy_send_timeout 7d;
        proxy_read_timeout 7d;
    }
}

# ===== ZS 管理员实例 (zs.xx.local) =====
server {
    server_name zs.xx.local;
    listen 80;

    location / {
        proxy_pass http://studio_zs;
        proxy_http_version 1.1;
        
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $server_name;
        
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        proxy_buffering off;
        proxy_cache off;
        proxy_request_buffering off;
        
        proxy_connect_timeout 7d;
        proxy_send_timeout 7d;
        proxy_read_timeout 7d;
    }
}
